import { ONBOARDING_DRAFT_CONTRACT_VERSION, PERMISSIONS, type OnboardingStep, type OnboardingStepOutcome } from "@miclub/shared";
import { Router, type Request } from "express";
import { requirePermission } from "../middleware/authorization.js";
import { onboardingService } from "../services/onboardingService.js";
import asyncHandler from "./asyncHandler.js";
import { isCompleteOnboardingRequest, isOpeningBalancesRequest } from "./onboardingValidation.js";
const router=Router();
const actor=(req:Request)=>({userId:req.auth!.userId,membershipId:req.auth!.membershipId,clubId:req.auth!.clubId,requestId:req.requestId,ip:req.ip,userAgent:req.get("user-agent")});
router.get("/onboarding",requirePermission(PERMISSIONS.ONBOARDING_READ),asyncHandler(async(req,res)=>res.json(await onboardingService.read(req.auth!.clubId))));
router.patch("/onboarding/advance",requirePermission(PERMISSIONS.ONBOARDING_WRITE),asyncHandler(async(req,res)=>{const body=req.body as Record<string,unknown>;if(Object.keys(body).some(k=>!["currentStep","outcome"].includes(k))||!Number.isInteger(body.currentStep)||Number(body.currentStep)<2||Number(body.currentStep)>7||!["COMPLETED","SKIPPED"].includes(String(body.outcome)))return res.status(400).json({code:"VALIDATION_ERROR",message:"Se requieren el próximo currentStep y un outcome válido"});try{return res.json(await onboardingService.advance(actor(req),body.currentStep as OnboardingStep,body.outcome as OnboardingStepOutcome));}catch(error){if(typeof error==='object'&&error&&'code'in error)return res.status(409).json({code:error.code,message:error instanceof Error?error.message:"Conflicto de onboarding"});throw error;}}));
router.post("/onboarding/opening-balances",requirePermission(PERMISSIONS.ONBOARDING_WRITE),asyncHandler(async(req,res)=>{if(!isOpeningBalancesRequest(req.body))return res.status(400).json({code:"VALIDATION_ERROR",message:"La moneda soportada, los saldos no negativos y la clave son obligatorios"});return res.json(await onboardingService.saveOpeningBalances(actor(req),req.body));}));
const onboardingErrorStatus=(code:string)=>code==='ONBOARDING_SCHEMA_UNAVAILABLE'?503:code.includes('ALREADY')||code.includes('CONFLICT')?409:code.includes('INVALID')||code.includes('FORBIDDEN')||code.includes('MISSING')?422:400;
router.post("/onboarding/complete",requirePermission(PERMISSIONS.ONBOARDING_WRITE),asyncHandler(async(req,res)=>{const version=(req.body as {draft?:{contractVersion?:unknown}})?.draft?.contractVersion;if(version!==undefined&&version!==ONBOARDING_DRAFT_CONTRACT_VERSION)return res.status(409).json({code:"ONBOARDING_DRAFT_VERSION_UNSUPPORTED",message:`Versión ${String(version)} no soportada; se requiere ${ONBOARDING_DRAFT_CONTRACT_VERSION}. Reiniciá el borrador.`});if(!isCompleteOnboardingRequest(req.body))return res.status(400).json({code:"VALIDATION_ERROR",message:"El borrador completo contiene datos inválidos"});try{return res.json(await onboardingService.complete(actor(req),req.body.draft));}catch(error){if(typeof error==='object'&&error&&'code'in error){const code=String(error.code);return res.status(onboardingErrorStatus(code)).json({code,message:error instanceof Error?error.message:"No se pudo completar el onboarding"});}throw error;}}));
export default router;
