import assert from "node:assert/strict";
import test from "node:test";
import { billingService } from "./billingService.js";
test("FREE y cada plan pago de pruebas quedan activos para resolver entitlements",()=>{const previous=process.env.NODE_ENV;process.env.NODE_ENV="test";try{assert.equal(billingService.prepareOnboardingSelection("FREE").status,"active");for(const code of ["SOCIAL","COMPLEX","CLUB"] as const)assert.equal(billingService.prepareOnboardingSelection(code).status,"active");}finally{process.env.NODE_ENV=previous;}});
test("producción no simula pagos sin flag y con flag conserva pending_payment",()=>{const node=process.env.NODE_ENV,flag=process.env.ONBOARDING_PAID_PLAN_SELECTION_ENABLED;process.env.NODE_ENV="production";delete process.env.ONBOARDING_PAID_PLAN_SELECTION_ENABLED;try{assert.throws(()=>billingService.prepareOnboardingSelection("SOCIAL"),(error:unknown)=>(error as {code?:string}).code==="PAID_PLAN_SELECTION_DISABLED");process.env.ONBOARDING_PAID_PLAN_SELECTION_ENABLED="true";assert.equal(billingService.prepareOnboardingSelection("CLUB").status,"pending_payment");}finally{process.env.NODE_ENV=node;if(flag===undefined)delete process.env.ONBOARDING_PAID_PLAN_SELECTION_ENABLED;else process.env.ONBOARDING_PAID_PLAN_SELECTION_ENABLED=flag;}});
